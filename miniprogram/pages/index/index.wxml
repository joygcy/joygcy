<view class="container">
  <view
    wx:for="{{list}}"
    wx:key="key" 
    class="item" 
    data-page="{{item.page}}"
    bindtap="onJumpTo"
  >  
    <t-icon name="{{item.icon}}" color="{{item.color}}" size="96rpx" />
    <view>{{item.label}}</view> 
    
  </view>
</view>